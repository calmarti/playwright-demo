{
  "name": "playwright-cucumber-e2e-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:all": "rm -rf reports && mkdir reports && cucumber-js features/**/*.feature --format html:reports/cucumber_report.html --format summary --format @cucumber/pretty-formatter --publish --publish-quiet",
    "test": "rm -rf reports && mkdir reports && cucumber-js --format html:reports/cucumber_report.html --format summary --format @cucumber/pretty-formatter --publish --publish-quiet",
    "report:local": "start reports/local_report.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cucumber/cucumber": "^7.0.0",
    "@cucumber/pretty-formatter": "^1.0.0-alpha.0",
    "@playwright/test": "^1.58.2",
    "@types/cucumber": "^6.0.1",
    "@types/node": "^25.5.0",
    "cucumber-html-reporter": "^5.3.0",
    "eslint": "^9.39.2",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.2"
  }
}
